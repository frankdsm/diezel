// Diezel - The simple React framework

// Re-export Hono for API routes
export { Hono } from 'hono'
export type { Context, Next } from 'hono'

// Framework types
export type { PageProps, LayoutProps } from './types'
